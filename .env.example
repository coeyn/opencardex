# Optional path to a SQLite snapshot used by the local Python server.
# POKEMON_TCG_TRACKER_DB=C:\path\to\tracker_snapshot.db

# Optional server bind settings planned for future config support.
# POKEMON_TCG_TRACKER_HOST=127.0.0.1
# POKEMON_TCG_TRACKER_PORT=8765
